      <!-- Navbar -->
      <nav class="main-header navbar navbar-expand navbar-success navbar-dark">
        <!-- Left navbar links -->
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
          </li>
        </ul>

        <!-- Right navbar links -->
        <ul class="navbar-nav ml-auto">
          <!-- Navbar Search -->


          <!-- Sidebar user (optional) -->
          <!-- <div class="nav-item user-panel mt-1 pb-1 mb-1 d-flex">
               <a class="nav-link d-block">Admin</a> -->



      <!-- Notifications Dropdown Menu -->
      <li class="nav-item dropdown">
        <a class="nav-link" data-toggle="dropdown" href="">
           <i class="nav-icon fas fa-user"></i>
                    
          @if (User.IsInRole("Administrador"))
            {
              <span class="">Administrador</span>
            }
            else
            {
              <span class="">Invitado</span>
            }
            
        </a>

          @if (User.IsInRole("Administrador"))
            {
        <div class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
          <a class="dropdown-item"  asp-area="" asp-controller="Usuarios" asp-action="Index">
            <i class="fas fa-user mr-2"></i> Mi perfil
          </a>
          <div class="dropdown-divider"></div>
          <a class="dropdown-item" asp-area="" asp-controller="Usuarios" asp-action="Index">
            <i class="fas fa-key mr-2"></i> Cambiar contraseña
          </a>
          <div class="dropdown-divider"></div>
          <a class="dropdown-item" asp-area="" asp-controller="Acceso" asp-action="Salir">
            <i class="fas fa-arrow-circle-right mr-2"></i> Cerrar sesión
          </a>
        </div>
      }
      </li>
          <!-- </div> -->

        </ul>
      </nav>
      <!-- /.navbar -->
