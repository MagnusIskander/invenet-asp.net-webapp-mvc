@model IEnumerable<INVENNET.Models.Usuario>

@{
    ViewData["Title"] = "Usuarios";
    int contador = 1;
}

<h1 style="text-align:center;">    Panel de control de usuarios</h1>
<br>
<br>
<div class="container" style="margin-top: 3%; margin-bottom: 5%;">
       @foreach (var item in Model) {
        @*<div class="row">
            <a href="#seccion" class="btn btn-info" data-toggle="collapse" style="width:300px;">    >> @Html.DisplayFor(modelItem => item.Usuario1)   -   @Html.DisplayFor(modelItem => item.Rol)</a>
            <div class="collapse" id="seccion">
                <br>
                <div class="well" style="text-align:center;">
                    <p>
                        <a class="btn-outline-primary" asp-action="Details" asp-route-id="@item.Id">Detalles</a> |
                        <a class="btn-outline-warning" asp-action="Edit" asp-route-id="@item.Id">Editar</a> |
                        <a class="btn-outline-danger" asp-action="Delete" asp-route-id="@item.Id">Eliminar</a>
                    </p>
                </div>
            </div>
        </div>
        <br />*@
        <div class="panel-group" id="accordeon_@contador" role="tablist">
            <div class="panel panel-default">
                <div class="panel-heading" role="tab" id="heading+@contador">
                    <h4 class="panel-tittle" style="text-align:center;">
                        <a class="btn btn-info" href="#collapse_@contador" data-toggle="collapse" data-parent="#accordeon_@contador" style="width:300px;">
                            @Html.DisplayFor(modelItem => item.Usuario1)   -   @Html.DisplayFor(modelItem => item.Rol)
                        </a>
                    </h4>
                </div>
                <div id="collapse_@contador" class="panel-collapse collapse in">
                    <div class="panel-body" style="text-align:center;">
                        <a class="btn btn-primary" asp-action="Details" asp-route-id="@item.Id">Detalles</a> |
                        <a class="btn btn-warning" asp-action="Edit" asp-route-id="@item.Id">Editar</a> |
                        <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Id">Eliminar</a>
                    </div>
                    <br />
                </div>
            </div>
        </div>
        
        contador++;
    }
</div>
<br />
<p style="text-align:center;">
    <a class="btn btn-success" asp-action="Create">Nuevo Usuario</a>
</p>



@*<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Usuario1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Clave)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rol)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nombres)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Apellidos)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fecha)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Usuario1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Clave)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rol)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nombres)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Apellidos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha)
            </td>
            <td>
                <a class="btn-outline-success" asp-action="Details" asp-route-id="@item.Id">Details</a>
                <a class="btn-outline-warning" asp-action="Edit" asp-route-id="@item.Id">Edit</a> | |
                <a class="btn-outline-danger" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>*@




<!-- <h1>@ViewData["Title"]</h1> -->

<p>
    <a class="btn btn-success" asp-action="Create">Registrar</a>
</p>

<!-- INICIO TABLA -->
<table id="example2" class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nombre de usuario</th>
            <th>Rol</th>
            <th>Nombres</th>
            <th>Apellidos</th>
            <th>Fecha de registro</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Usuario1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rol)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nombres)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Apellidos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha)
            </td>
            <td>
                <a class="btn btn-primary" asp-action="Details" asp-route-id="@item.Id">Detalles</a> | <a class="btn btn-warning" asp-action="Edit" asp-route-id="@item.Id">Editar</a>

 | <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Id">Eliminar</a>
                
            </td>
        </tr>
}
    </tbody>
    <tfoot>
        <tr>
            <th>Id</th>
            <th>Nombre de usuario</th>
            <th>Rol</th>
            <th>Nombres</th>
            <th>Apellidos</th>
            <th>Fecha de registro</th>
            <th>Acciones</th>
        </tr>
    </tfoot>
</table>
<!-- /.FIN TABLA -->
